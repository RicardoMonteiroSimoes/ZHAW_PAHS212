function [latex] = MatrixToLatex(matrix)
    latex = "\begin{pmatrix}";
    for i = length(matrix)
        for j = length(matrix(1,:))
            l = split("\" + string(symvar(m(i,j))),"_");
            latex = latex +
            if j < length(matrix(1,:))
                latex = latex + " & ";
        end
        latex = latex + "\\"
    end
    latex = latex + "\end{pmatrix}";
end

